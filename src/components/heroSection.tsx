import SectionWrapper from "./section-wrapper";

const HeroSection = () => {
  return (
    <SectionWrapper>
      <div className="h-[100vh]"></div>
    </SectionWrapper>
  );
};

export default HeroSection;
